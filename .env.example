# Optional: only needed if you want to persist submitted tasks/results.
DATABASE_URL="postgresql://user:password@host:port/dbname"

# Optional UI theme: indigo, emerald, rose, amber, violet, light, dark.
NEXT_PUBLIC_THEME=indigo
